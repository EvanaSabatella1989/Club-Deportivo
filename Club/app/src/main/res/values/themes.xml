<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Club" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/yellow</item>
      </style>

      <style name="Theme.Club" parent="Base.Theme.Club" />

    <style name="Theme.Club.NoTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Personalizaciones del tema específico -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorAccent">@color/black</item>
    </style>
  </resources>